package org.example.MyWitcher.solid.o.exm1.good;

public class LoanApprovalHandler {
    public void approveLoan(Validator validator) {
        if (validator.isValid()) {
            // process the loan
        }
    }
}
